name = "sankalp is  a good"

print(name.find("  "))

# The find() method returns the lowest index of the substring if it is found in given string. If it's not found then it returns -1.

print(name.replace("  ", " "))

print(name)

# The string is not changed because strings are immutable . if we run function on string it will return a new string. the main string will reamin unchanged.

